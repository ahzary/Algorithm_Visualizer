#include "../AlgorithmClass.h"
#include <QDebug>
/*
class MyAlgorithm : public Algorithm {
public:
    void algorithmRun() override {
        qDebug() << "MyAlgorithm is running!";
    }

    void resetAlgorithm() override {
        stepsTaken = 0;
        qDebug() << "Algorithm reset!";
    }

    void set_Steps_per_seconds(int stps) override {
        steps_per_seconds = stps;
        qDebug() << "Steps per second set to" << stps;
    }
};


extern "C" DLL_EXPORT Algorithm*  createAlgorithm() {
    return new MyAlgorithm();
}
*/
